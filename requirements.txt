# QML Agentic RAG — dependencies
# LLM is fully on OpenRouter; GPU is used only for embeddings

# LangChain / LangGraph
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-openai>=0.2.0          # OpenRouter uses OpenAI-compatible API
langchain-huggingface>=0.1.0     # for bge-large-en-v1.5 embeddings only

# Embeddings (local, ~1.3 GB VRAM on cuda)
sentence-transformers>=3.0.0
torch>=2.2.0

# PDF ingestion
pypdf>=4.0.0

# Utilities
pydantic>=2.0.0
tqdm>=4.66.0
pandas>=2.0.0
pymupdf
python-dotenv
faiss-cpu
grandalf
IPython